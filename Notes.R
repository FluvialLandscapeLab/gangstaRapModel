# Identifier could be a regular expression- use ls() to get names of all variables to update
# Then use paste to make the value expressions
# List(ID, Value) or List(ID, Value, Pattern), if there is a pattern, turn into list of expressions
# Calculator blocks that calculate on entire groups of variables
# First make it work with one leak in